#ifndef _AOC_STRINGS_H_
#define _AOC_STRINGS_H_

#include <vector>
#include <string>

/* Splits a string based off a delimiter 
 * Returns a vector of substrings
*/
std::vector<std::string> 
splitString(std::string str, std::string delimiter);

#endif